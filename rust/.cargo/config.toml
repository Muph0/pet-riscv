[build]
target = "riscv32im-unknown-none-elf"

[target.riscv32im-unknown-none-elf]
rustflags = ["-C", "link-arg=-Tlink.x"]

[alias]
hello-bin = "rust-objcopy --release --bin hello -- -O binary --only-section=.text --only-section=.rodata --only-section=.data ./hello.bin"
